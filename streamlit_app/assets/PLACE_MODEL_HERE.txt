# Place your trained model files here:
#
#   best.pt    — YOLOv8n PyTorch weights  (from Kaggle Notebook 1)
#   best.onnx  — ONNX export              (preferred for Streamlit)
#
# Download from:
#   /kaggle/working/runs/ingredient_yolov8n/weights/best.pt
#   /kaggle/working/best.onnx
#
# Until the model is trained, the app runs in DEMO MODE
# with mock detections (see components/detector.py).
